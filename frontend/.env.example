# =====================================================
# Lumina Host — Frontend (.env.example)
# Copie este arquivo para `.env` antes de rodar.
# =====================================================

# URL da API backend. Em produção, aponte para o domínio público (ex: https://api.seudominio.com/api).
VITE_API_URL=http://localhost:3003/api
